<div class="home-head">
  <div class="container">
    <div class="home-head__container">
      <h1 class="home-head__title" data-text="Handpicked Albums"><span class="glitch" data-text="Handpicked">Handpicked</span> <span class="glitch" data-text="Albums">Albums</span></h1>
      <p class="home-head__subtitle">by the Glitch Family</p>
    </div>
  </div>
</div>

<div class="content">
  <div class="container">
    <div class="grid grid-catalog">
      <%= for album <- @albums do %>
      <a class="catalog-item" href="<%= Routes.album_path @conn, :show, album.hash, album.slug%>">
        <h2 class="catalog-item__album"><%= album.name%></h2>
        <div class="catalog-item__artist">by <%= album.artist%></div>
        <img class="catalog-item__visual" src=<%= album.cover %> alt="">
      </a>
      <% end %> 
    </div>
  </div>
</div>

<div class="pagination">
  <div class="container">
    <%= if last_page?(@page) do %>
      <a class="btn btn--primary btn--big w100 disabled" href="">Load moaaaar</a>
    <% else %>
      <a class="btn btn--primary btn--big w100" href="<%= Routes.page_path(@conn, :page, @page.page_number+1) %>">Load moaaaar</a>
    <% end %>
    <div class="pagination__side">
      <%= if first_page?(@page) do %>
        <span class="btn btn--secondary w100 disabled" href="">Prev</span>
      <% else %>
        <a class="btn btn--secondary w100" href="<%= Routes.page_path(@conn, :page, @page.page_number - 1)%>">Prev</a>
      <% end %>

      <%= if last_page?(@page) do %>
        <a class="btn btn--secondary w100 disabled" href="">Next</a>
      <% else %>
        <a class="btn btn--secondary w100" href="<%= Routes.page_path(@conn, :page, @page.page_number+1) %>">Next</a>
      <% end %>
    </div>
  </div>
</div>

<section class="home-about">
  <div class="container">
    <h2 class="visually-hidden">About</h2>
    <p>At Glitch Family we love music, I mean we LOVE music! And we also LOVE great album covers. So we decided to make a place where we can showcase albums that are dear to us.</p>
    <p>
      This is Now Playing.<br>
      A Glitch Family’s project.
    </p>
  </div>
</section>
